package com.recon.processing.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class ReconBatchConfig {
    // Batch job wiring is intentionally deferred; processors/readers/writers are available for service orchestration.
}
